/**
 * @deprecated — dùng loveTreeState.js (độ thân mật = cây tình yêu, không tính từ tin nhắn)
 */
export {
  canSuggestDateFromTree as canSuggestDate,
  loveTreeToDisplayState as getIntimacyState,
  DATE_INVITE_MIN_LEVEL,
  DATE_INVITE_MIN_ATTACHMENT,
} from './loveTreeState.js'
